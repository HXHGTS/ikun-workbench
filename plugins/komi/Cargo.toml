[package]
name = "tauri-plugin-komi"
version = "1.0.0"
edition = "2021"
links = "tauri-plugin-komi"

[dependencies]
tauri = { version = "2.9.4" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }
